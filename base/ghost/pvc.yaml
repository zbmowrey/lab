apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ghost-uploads
  namespace: ghost
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: openebs
