apiVersion: v1
kind: Service
metadata:
  name: docmost
  labels:
    app: docmost
spec:
  type: ClusterIP
  selector:
    app: docmost
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
