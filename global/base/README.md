# About

This subdirectory is more for "global" or "shared" infrastructure or components, and thus doesn't rely on
namespace from kustomization.yaml. Each resource here should define its target namespace explicitly. 